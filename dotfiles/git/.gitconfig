# ~/.gitconfig
# Minimal, safe defaults. user.name and user.email are set by bootstrap.sh.

[init]
  defaultBranch = main

[pull]
  rebase = true

[core]
  editor = vim
  autocrlf = input

[fetch]
  prune = true

[diff]
  colorMoved = zebra

[rerere]
  enabled = true
